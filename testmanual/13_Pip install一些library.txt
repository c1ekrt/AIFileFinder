//=== 更新dataset ======
pip install diffusers --upgrade
pip install datasets --upgrade 
//pip install diffusers[training] accelerate datasets 
accelerate config             // 多GPU,可能尚未完整
//accelerate launch 51_Gene_LoRA_Hug.py

//--- XL -----
pip install invisible_watermark transformers accelerate safetensors
  
//=== 其他 =========================
pip3 show torch
pip3 install --upgrade pip 
pip list 
pip install NumPy==1.24          Numba要求NumPy 1.24或以下. 目前為1.24.1
pip install accelerate          pip uninstall accelerate         pip install --upgrade accelerate
pip install matplotlib
pip install transformers 
pip install fastdownload 
pip install torchsummary
pip install pynvml               // check cuda memory   
pip install scipy
pip install xformers --upgrade
pip install speechbrain
pip install playsound==1.2.2     //play mp3, 最新1.3.0(有)
pip install fairseq
pip install g2p_en
pip install g2pc
pip install hanziconv
pip install tqdm --upgrade
pip install gradio --upgrade      // pip install gradio==3.48.0 for diffuser
pip install modules
pip install omegaconf           pip uninstall omegaconf
pip install moviepy
pip install fastparquet

//--- ollama ------
pip install ollama                // 需先pip install ollama,再用ollama.exe install   
pip install gradio --upgrade      // pip install gradio==4.29.0 for translate
pip install pyodbc

//--- translate ------
pip install google.generativeai
pip install pyopencc
pip install google-cloud-translate==2.0.1  // no work
pip install --upgrade google-auth          // no work
 
#--- RVC -----
pip install -r requirements.txt   
pip install fairseq 
pip install av
pip install python-dotenv
pip install faiss-cpu
pip install praat-parselmouth
pip install pyworld
pip install torchcrepe
pip install ffmpeg-python         // pip uninstall ffmpeg, pip uninstall ffmpeg-python 
pip install rembg-python
pip install playsound==1.2.2
pip install pygame               //play wav, 
pip install seaborn
pip install pydsm   

#pip install bitsandbytes         // 不是windows版本, pip uninstall bitsandbytes
pip install bitsandbytes-windows  // use_8bit_adam    pip uninstall bitsandbytes-windows 
pip install git+https://github.com/Keith-Hon/bitsandbytes-windows.git
https://github.com/TimDettmers/bitsandbytes/issues/175  ??????

//--- video -----
pip install imageio opencv-python       pip install opencv-python  不知何者??需要降到OpenCV(4.7.0)
pip install imageio[pyav]
pip install imageio[ffmpeg]

//--- audio -----
pip install pyaudio
pip install spleeter         pip uninstall spleeter


//--- ControlNet ------  
pip install opencv-contrib-python
pip install controlnet_aux --upgrade
pip install mediapipe
  
//--- XGBoost ---------
pip install duckdb
pip install rdkit

